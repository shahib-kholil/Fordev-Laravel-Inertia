import { Link } from '@inertiajs/react';
import PublicLayout from '@/layouts/public-layout';

export default function Home({ webServices, portfolios, testimonials }) {
    return <PublicLayout title="ForDev" description="Jasa pembuatan website, domain, portofolio, dan landing page."><section className="mx-auto max-w-6xl px-4 py-20"><div className="max-w-2xl space-y-6"><h1 className="text-4xl font-bold md:text-6xl">Website bisnis yang cepat dibuat dan mudah dikelola.</h1><p className="text-lg text-muted-foreground">ForDev bantu UMKM dan bisnis punya website profesional, domain, dan halaman penawaran yang siap dipakai.</p><div className="flex gap-3"><Link href="/order" className="rounded-lg bg-primary px-4 py-2 text-primary-foreground">Minta Penawaran</Link><Link href="/portofolio" className="rounded-lg border px-4 py-2">Lihat Portofolio</Link></div></div></section><Section title="Paket Jasa">{webServices.map((item) => <Card key={item.id} title={item.name} subtitle={`Rp ${Number(item.price).toLocaleString('id-ID')}`} text={item.description} />)}</Section><Section title="Portofolio Unggulan">{portfolios.map((item) => <Card key={item.id} title={item.title} subtitle={item.category} text={item.description} />)}</Section><Section title="Testimoni">{testimonials.map((item) => <Card key={item.id} title={item.client_name} subtitle={item.client_role} text={item.content} />)}</Section></PublicLayout>;
}
function Section({ title, children }) { return <section className="mx-auto max-w-6xl px-4 py-10"><h2 className="mb-4 text-2xl font-semibold">{title}</h2><div className="grid gap-4 md:grid-cols-3">{children}</div></section>; }
function Card({ title, subtitle, text }) { return <article className="rounded-xl border p-5"><h3 className="font-semibold">{title}</h3><p className="mt-1 text-sm text-muted-foreground">{subtitle}</p><p className="mt-3 text-sm">{text}</p></article>; }
