import PublicLayout from '@/layouts/public-layout';
import Pagination from '@/components/pagination';
export default function Domains({ domains }) { return <PublicLayout title="Katalog Domain"><div className="mx-auto max-w-6xl px-4 py-12"><h1 className="mb-6 text-3xl font-semibold">Katalog Domain</h1><div className="overflow-hidden rounded-xl border"><table className="w-full text-sm"><thead className="bg-muted/60 text-left"><tr><th className="p-3">Ekstensi</th><th className="p-3">Harga</th></tr></thead><tbody>{domains.data.map((item) => <tr key={item.id} className="border-t"><td className="p-3 font-medium">{item.extension}</td><td className="p-3">Rp {Number(item.price).toLocaleString('id-ID')}</td></tr>)}</tbody></table></div><div className="mt-6"><Pagination links={domains.links} /></div></div></PublicLayout>; }
