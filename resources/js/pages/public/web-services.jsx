import { Link } from '@inertiajs/react';
import PublicLayout from '@/layouts/public-layout';
import Pagination from '@/components/pagination';
export default function WebServices({ webServices }) { return <PublicLayout title="Katalog Jasa Web"><div className="mx-auto max-w-6xl px-4 py-12"><h1 className="mb-6 text-3xl font-semibold">Katalog Jasa Web</h1><div className="grid gap-4 md:grid-cols-3">{webServices.data.map((item) => <article key={item.id} className="rounded-xl border p-5"><h2 className="text-lg font-semibold"><Link href={`/jasa-web/${item.slug}`}>{item.name}</Link></h2><p className="text-muted-foreground">Rp {Number(item.price).toLocaleString('id-ID')}</p><p className="mt-3 text-sm">{item.description}</p><ul className="mt-3 list-inside list-disc text-sm">{item.features.map((feature) => <li key={feature}>{feature}</li>)}</ul></article>)}</div><div className="mt-6"><Pagination links={webServices.links} /></div></div></PublicLayout>; }
